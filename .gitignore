# Python
venv/
__pycache__/
*.pyc

# Environment / secrets
.env

# Local databases and app instance data
*.db
instance/

# User uploads (runtime data, not source)
uploads/

# Runtime locks
*.lock

# Claude Code worktrees
.claude/worktrees/

# Tailwind build tooling (CLI downloaded at build time, CSS is committed)
tailwind-cli.exe
tailwind-cli*
